name: Build Image
on:
  push:
    branches:
    - main
jobs:
  build_image:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v1
    - name: Echo test
      run: |
        echo "Test1"
        echo "Test2"
        docker build -t plumbertest .